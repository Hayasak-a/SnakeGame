#include "game.h"
#include <stdlib.h>
#include <stdio.h>
#include <ncurses.h>
#include <time.h>

int main(void){
  srand(time(NULL));
    printf("thuoeou");
    game(3, 0);
    printf("tuenao");
    return(0);
}